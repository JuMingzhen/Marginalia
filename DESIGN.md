# Reader — 本地 PDF 阅读与笔记桌面应用 · 设计文档

> 状态：设计稿 v2（2026-07-28）
> 形态：Windows 原生桌面程序，纯本地、离线优先。
> 核心价值：**读得舒服** + **笔记可回溯**。

---

## 1. 目标与非目标

### 目标
1. **优质阅读体验**：连续滚动、清晰渲染、护眼配色、目录导航、进度记忆、全文搜索。
2. **低摩擦笔记**：读到有意思的地方，两三个动作内记完，不打断阅读节奏。
3. **强回溯能力**：任一条笔记都能答出三个问题——*哪本书哪一页？原文说了什么？我当时想了什么？*
4. **扫描版一等公民**：无文字层的书不降级，靠区域截图 + OCR 补齐。
5. **随手打开**：双击 PDF 文件即用本程序打开；程序内也能按路径浏览打开。

### 非目标（v1 不做）
- 云同步、账号、多设备
- 数据库（全部落地为 JSONL / JSON / PNG）
- 改写原 PDF 文件（笔记与原文件解耦，**原文件永不修改**）
- EPUB / MOBI

### 硬约束
- 默认完全离线。唯一联网点是**用户显式配置 API Key 后**的 LLM 翻译/解释（和可选云 OCR）。
- 未配置 Key 时程序不发出任何网络请求。

---

## 2. 技术选型

| 层 | 选择 | 说明 |
|---|---|---|
| GUI | **PySide6 (Qt 6)** | 原生窗口、原生文件对话框、成熟的绘制与滚动 |
| PDF 引擎 | **PyMuPDF (MuPDF)** | 渲染、词框坐标、文本抽取、搜索、目录，一个库全包 |
| 本地 OCR | **RapidOCR (ONNXRuntime)** | 中英双语、CPU 可跑、离线、模型约 15MB |
| LLM | Anthropic SDK（可插拔，兼容 OpenAI 协议） | 划词翻译/解释 |
| 存储 | JSONL / JSON / PNG | 文件系统即数据库 |
| 打包 | PyInstaller → 单个 `Reader.exe` | |

### 为什么是 Qt 而不是 Electron / Tauri

去掉浏览器这个前提后，选型逻辑整个反转了：

- **原方案里选 PDF.js，是因为要拿文字层做划词。但 PyMuPDF 的 `get_text("words")` 直接给出每个词的精确矩形**——文字层的地基它自己就有，PDF.js 不再是必需品。
- **原方案里必须有 Python，是因为 OCR 和高清裁剪只能在 Python 做。** 这条没变。
- 于是 Electron 方案 = Electron 前端 + Python 后端 **两套运行时 + 一层 IPC + 两次打包**；Qt 方案 = **一个 Python 进程**。

对一个自用工具，这个差距是决定性的。代价是文本选择的拖选逻辑要自己写（约 300 行，见 §5.2），但换掉的是整个跨语言 IPC 层和打包地狱。

MuPDF 的渲染质量本身也不输 PDF.js——它是 SumatraPDF、mupdf、Zathura 等一票阅读器的底层引擎。

---

## 3. 数据模型

### 3.1 目录布局

```
%USERPROFILE%\.reader\             # 数据根目录，可配置
  config.json                      # API Key、主题、OCR 后端、数据目录
  library.jsonl                    # 书库索引，一行一本
  docs/
    d_9f2a1c4b8e3d/                # doc_id = sha256(文件内容前 8MB + 文件大小)[:12]
      meta.json                    # 标题、作者、页数、原文件路径、是否有文字层
      notes.jsonl                  # 笔记事件流（append-only）
      progress.json                # 阅读进度
      thumb.png                    # 封面缩略图（书库网格用）
      clips/  n_01JQ8F....png      # 区域笔记的高清截图
      ocr/    p0012.json           # 整本预 OCR 的页级缓存（词框 + 文本）
```

**`doc_id` 用内容哈希而非路径**：书挪了位置、改了文件名，笔记照样认得出来。`meta.json` 记最后已知路径，找不到时提示重新定位。哈希只取前 8MB + 文件大小，避免每次打开大书都全文件读一遍。

### 3.2 笔记记录

```jsonc
{
  "id": "n_01JQ8F3K2M9XQZ",          // ULID，按时间自然有序
  "doc_id": "d_9f2a1c4b8e3d",
  "created_at": "2026-07-28T10:11:12+08:00",
  "updated_at": "2026-07-28T10:15:40+08:00",

  "anchor": {
    "kind": "text",                   // "text" | "region"
    "page": 42,                        // 0-based
    "rects": [[72.0, 310.5, 523.2, 326.1]],   // PDF 用户空间坐标 (pt)，逐行一个
    "word_range": [104, 121]           // 仅 text：页内词序号区间，用于精确复原选区
  },

  "quote": "Attention is all you need.",   // 原文（选中文字 或 OCR 结果）
  "quote_source": "textlayer",             // "textlayer" | "ocr" | "manual"
  "clip": "clips/n_01JQ8F3K2M9XQZ.png",    // 仅 region

  "body": "这里其实回答了我上周那个疑问：为什么 RNN 会被替代。",   // 我的想法，Markdown
  "color": "yellow",
  "tags": ["transformer", "todo"],

  "ai": [                                  // AI 产出与手写严格分开
    { "kind": "translate", "model": "claude-opus-5",
      "text": "注意力就是你所需要的一切。", "at": "2026-07-28T10:12:00+08:00" }
  ]
}
```

**为什么 `ai` 单开一个数组？** 半年后回看笔记，你必须一眼分得清哪句是自己想的、哪句是机器给的。混进 `body` 里就永远分不清了。

**坐标为什么用 PDF pt？** 与缩放、DPI、窗口大小无关，PyMuPDF 原生就是这个坐标系。PDF 排版固定（不像 EPUB 回流），页码 + 矩形是永久有效的锚点。

### 3.3 JSONL 是事件流，不是表

`notes.jsonl` 每行是一个**事件**，只追加不改写：

```jsonl
{"op":"create","at":"...","note":{...}}
{"op":"update","at":"...","id":"n_01JQ8F...","patch":{"body":"改了想法"}}
{"op":"delete","at":"...","id":"n_01JQ8F..."}
```

打开书时回放得到当前状态，常驻内存。好处：
- 写入永远是 O(1) 追加，不会因为改一条笔记而重写整个文件，也就写不坏
- 天然带修改历史（"我三个月前是怎么想的"本身有价值）
- 崩溃/断电最多丢最后一行

超过 2000 行时后台 compaction，旧文件留档为 `notes.jsonl.1`。

---

## 4. 交互设计

### 4.1 打开书的三条路

1. **启动页 = 书库**：最近阅读排前，封面网格 + 阅读进度条，双击继续读。
2. **原生文件对话框**：`Ctrl+O` → `QFileDialog`，从任意路径开，记住上次目录。窗口内拖入 PDF 也直接打开。
3. **系统文件关联**：安装时可选注册 `.pdf` 的「打开方式」，之后在资源管理器双击/右键即可用本程序打开。首次打开的书自动入库。

### 4.2 有文字层：划词 → 批注

```
拖选文字 → 就地浮出小工具条（5 色 + 批注 + 翻译）
        → 点颜色：立即高亮存盘，不打断阅读        ← 最快路径，1 次点击
        → 点批注：右栏滑出笔记卡，原文已填好，光标停在输入框
键盘流   → 选中后按 h 直接高亮，按 n 直接开批注
```

原则：**最常用的动作只要一次点击。** 想深写的时候再展开。

### 4.3 扫描版：框选 → OCR → 笔记

你特别强调的路径，做成一等流程：

```
按住 Alt 拖框（或点工具栏「框选」）
  → 松手瞬间：右栏弹出笔记卡，顶部是刚裁下的图（后台按 300 DPI 重新渲染那块区域）
  → 同时对这块图跑 OCR，识别中显示骨架条
  → OCR 返回后填入「原文」框，可直接编辑修正
  → 你在下方写自己的想法 → 保存
```

三个关键点：
- **OCR 异步，绝不阻塞**。卡片立刻弹出，你可以马上开始打字，OCR 结果晚一两秒填进来。
- **只 OCR 选中的那一小块，且按 300 DPI 重渲**。屏幕上那张图只有 ~100 DPI，直接拿去识别准确率很差；局部高 DPI 重渲是两个量级的差别。
- **OCR 结果可编辑**。把它当草稿，不当真理。

### 4.4 整本预 OCR（后台任务）

扫描版可一键"为整本建立文字层"：后台逐页 OCR，结果存 `ocr/pNNNN.json`。完成后：
- 词框喂给选择层 → **扫描版也能像正常 PDF 一样划词高亮**
- 全文搜索可用

几百页可能十几分钟，做成可中断、可续跑、状态栏带进度。**局部 OCR 和整本 OCR 两条路都要有**：只做前者则扫描版永远搜不了；只做后者则每本书都要先等十几分钟。

### 4.5 回溯：三个入口

需求里"事后可追溯笔记地点以及用户的感想"这半句最容易做塌，所以给三条路：

1. **当前书侧栏**：按页排序的笔记列表，点击 → 平滑滚到该页，高亮闪两下定位。
2. **全局笔记浏览器**（`Ctrl+Shift+F`）：跨所有书，按标签 / 时间 / 关键词筛。回答"我在哪本书见过 XXX"。
3. **导出 Markdown**：每条带回跳深链

   ```markdown
   > Attention is all you need.

   这里其实回答了我上周那个疑问……

   — 《Attention Is All You Need》 [p.42](reader://doc/d_9f2a1c4b8e3d?note=n_01JQ8F3K2M9XQZ)
   ```

   安装时注册 `reader://` 自定义协议（Windows 注册表几个键），丢进 Obsidian 的笔记点一下就跳回原文位置。

### 4.6 阅读体验细节

- **连续滚动 + 虚拟化**：只渲染视口 ±2 页，pixmap 走 LRU 缓存（按内存上限逐出），几百页的书内存稳定
- **渲染倍率**跟随 `devicePixelRatio`，高分屏不糊
- **配色三档**：原色 / 纸色（暖白底）/ 夜间。夜间对文字版做反色 + 色调回正；扫描版单独一套曲线，避免插图被反成负片
- **视图**：单页 / 双页 / 适配宽度 / 适配整页
- **进度**：记 `{page, y_ratio}`，退出即存，重开回到原处
- **键盘**：`j/k` 滚动、`空格` 翻页、`g` 跳页、`Ctrl+F` 搜索、`h` 高亮、`n` 批注、`Alt+拖` 框选、`Ctrl+B` 侧栏

---

## 5. 架构与关键实现

### 5.1 模块划分

```
reader/
  app/       __main__.py  config.py  paths.py
  core/      document.py   # fitz.Document 封装、页几何、outline
             doc_id.py     # 内容哈希
             textmap.py    # ★词框 → 选择、命中测试、行合并
             render.py     # ★渲染线程 + LRU pixmap 缓存
             search.py     # 全文检索（文字层 + OCR 缓存）
  store/     library.py  notes.py  progress.py  clips.py
  services/  ocr/{base,rapid,cloud}.py   ai/anthropic.py   export.py
  ui/        main_window.py   library_view.py
             page_view.py     # ★虚拟滚动画布，核心
             overlays.py      # 选择层 / 高亮层 / 框选层
             note_editor.py   # 笔记卡（截图 + 原文 + 想法）
             notes_panel.py  outline_panel.py  search_panel.py
             global_notes.py  theme.py
```

三个 ★ 是工程重心，其余都是常规装配。

### 5.2 文本选择（唯一需要自己造的轮子）

PyMuPDF `page.get_text("words")` 返回 `(x0,y0,x1,y1, word, block, line, word_no)`，已按阅读顺序排好。于是：

```
mouseDown  → 命中测试找到起始词索引 i
mouseMove  → 找到当前词索引 j
选区       = words[min(i,j) .. max(i,j)]
高亮矩形   = 按 (block, line) 分组后逐行取并集   # 每行一个矩形，跨行自然分段
选中文本   = 空格拼接（同行）+ 换行拼接（跨行）
```

命中测试用按行分桶的二分，几千个词也是微秒级。约 300 行，是整个项目唯一有算法含量的地方。

**扫描版走同一条路**：整本预 OCR 后把 OCR 词框塞进同一个 `textmap`，选择逻辑完全复用，上层无感知。

### 5.3 线程模型（最大的工程风险）

PyMuPDF 的 `Document` 对象**不是线程安全的**，跨线程共用同一个句柄会崩。方案：

| 线程 | 持有 | 职责 |
|---|---|---|
| 主线程 (UI) | `Document` A | 页几何、词框查询、搜索、outline —— 都是快操作 |
| 渲染线程 | `Document` B（独立打开同一文件） | 页面渲染、区域高清裁剪，QImage 经信号回传 |
| OCR 线程池 | 无 | 只吃图片，与 PDF 无关 |
| AI 线程 | 无 | 流式返回，逐段发信号 |

关键：**同一文件打开两个独立 `Document` 句柄**，各自线程独占，零锁竞争。渲染结果通过 Qt 信号跨线程传回（`QImage` 可安全跨线程传递）。

### 5.4 打包注意

PyInstaller 打出来大约：PySide6 ~120MB + PyMuPDF ~20MB + onnxruntime ~50MB + OCR 模型 ~15MB。总计 **200MB+**。

建议 OCR 模型不打进 exe，首次使用时从本地路径加载或提示下载——**这样没有 OCR 需求的纯文字版用户拿到的是一个 150MB 的干净程序**，也保住了"离线可用"这条底线（模型可离线拷贝）。

---

## 6. 里程碑

| 阶段 | 内容 | 完成标准 | 状态 |
|---|---|---|---|
| **M1 · 能读** | 主窗 + 虚拟滚动渲染 + 目录 + 进度 + 缩放 + 配色 + 文件打开 | 能舒服地读完一本技术书 | ✅ 完成 |
| **M2 · 能记** | 划词选择 + 高亮/批注 + JSONL 存储 + 侧栏列表 + 点击回跳 | 笔记闭环打通 | ✅ 完成 |
| **M3 · 扫描版** | 框选截图 + 局部 OCR + 笔记卡整合 | 扫描版书可用 | ✅ 完成 |
| **M4 · 能找** | 全文搜索 + 整本预 OCR + 书库页 + 全局笔记浏览器 + Markdown 导出 | 回溯能力完整 | |
| **M5 · AI 辅助** | 划词翻译/解释，结果一键存为笔记 | 联网功能，可整体关闭 | |
| **M6 · 打包** | PyInstaller + 文件关联 + `reader://` 协议注册 | 双击 PDF 即可打开 | |

M1 + M2 是最小可用产品。**M3 是你的关键场景，优先级高于 M4。**

### 已知取舍（v1 有意为之，不是遗漏）

- **选择粒度是词，不是字符。** 选不出半个单词，但拖动时不需要像素级精准，
  划句子的手感反而更稳。
- **选区不跨页。** 鼠标滑到下一页时选区仍限制在起始页内。跨页选择要处理页间断裂，
  收益不抵复杂度。
- **阅读顺序取决于 PDF 自身的结构。** 按 (block, line, word) 排序保持的是文档生成时
  的顺序，MuPDF 默认不做分栏识别。结构混乱的 PDF 划词顺序也会乱，这是这条路线的固有上限。

---

## 7. 待定问题

1. **导入策略**：PDF 拷贝进 `.reader/docs/` 统一管理，还是只记原路径引用？（引用省空间，但文件被删就断链；也可两者都支持，导入时选）
2. **数据根目录**：默认 `%USERPROFILE%\.reader`。要不要放到能被 git / 网盘备份的位置？
3. **划词翻译的留痕**：翻译结果是弹浮层即用即弃，还是默认也落一条笔记？
4. **开发工位**：代码在 WSL 编辑，运行需在 Windows 侧装 Python 3.13 + 依赖。要不要我顺手写一份 Windows 侧的 `setup.ps1` 和运行脚本？
